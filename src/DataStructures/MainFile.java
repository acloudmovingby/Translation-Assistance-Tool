package DataStructures;


/**
 *
 * Now is unnecessary. Originally MainFile objects had some different methods than BasicFile, but now that is no longer true, but for organizational reasons, class was left as is.
 * 
 * @author Chris
 */
public class MainFile extends BasicFile {

    public MainFile(BasicFile file) {
        super(file);
    }

}
